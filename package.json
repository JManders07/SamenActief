{
  "name": "samenactief",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "client",
    "server",
    "shared"
  ],
  "scripts": {
    "build": "npm install && cd server && npm run build",
    "start": "cd server && npm start",
    "dev": "cd server && npm run dev"
  },
  "dependencies": {
    "cloudinary": "^2.0.1"
  }
}
